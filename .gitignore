# Private repo — working data (creds, targets, scan results) is committed too.

# The two original standalone projects this tool was fused from each have their
# OWN git repository. They are kept out of THIS repo to avoid a broken
# nested-repository state (see note from the assistant about submodules).
Script Project/
VNC scanner/

# Local tooling / compiled junk (not project content)
__pycache__/
*.pyc
.claude/
